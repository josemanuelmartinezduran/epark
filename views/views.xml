<flectra>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="epark.list">
      <field name="name">epark list</field>
      <field name="model">epark.epark</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="epark.action_window">
      <field name="name">epark window</field>
      <field name="res_model">epark.epark</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="epark.action_server">
      <field name="name">epark server</field>
      <field name="model_id" ref="model_epark_epark"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="epark" id="epark.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="epark.menu_1" parent="epark.menu_root"/>
    <menuitem name="Menu 2" id="epark.menu_2" parent="epark.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="epark.menu_1_list" parent="epark.menu_1"
              action="epark.action_window"/>
    <menuitem name="Server to list" id="epark" parent="epark.menu_2"
              action="epark.action_server"/>
    -->
  </data>
</flectra>